<div class="container mt-4">

  <%= link_to 'add state', new_board_state_path(@board), method: :post, class: 'btn btn-warning' %>
  <%= render partial: 'board', object: @board%>

</div>  


  <%= link_to 'Edit', edit_board_path(@board), class: 'btn btn-warning' %>  
  <%= link_to 'Destroy', board_path(@board), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?' } , class: 'btn btn-danger' %>
  <%= link_to 'Back to Boards', boards_path(@board), class: 'btn btn-secondary' %>
